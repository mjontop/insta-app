import React from "react";
import CreatePost from "../../Components/Post/CreatePost";

const upload = ({ profilePic }) => {
  return <CreatePost profilePic={profilePic} />;
};

export default upload;
